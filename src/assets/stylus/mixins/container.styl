container($fluid = false)
  padding: 0 (5 * $unit);
  margin: 0 auto;

  if $fluid {
    max-width: 100%;
  } else {
    max-width: ($container-max + $gutter * 2);
  }

  +breakpoint(ms-and-down) {
    padding: 0 $gutter;
  }
